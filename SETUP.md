# Hashpop Setup

Use the latest docs:

- Project overview and env reference: `README.md`
- Production deployment guide: `DEPLOY.md`

Quick local bootstrap:

```bash
npm install
npm install --prefix frontend
npm install --prefix backend
npm run compile
npm run deploy:testnet
npm run db:migrate
npm run dev
```
